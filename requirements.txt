# Dependencies for build_charts.py
#   Install with:  pip install -r requirements.txt
# Only these two are imported directly; numpy, python-dateutil, pytz, six and
# tzdata come along automatically as pandas dependencies.

pandas==2.3.3   # reads the EIA .xls, merges series, computes the 3-2-1 spread
xlrd==2.0.2     # legacy .xls engine pandas needs for EIA's file format
